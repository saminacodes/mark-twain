Edit documentation files within this repo for consistency, clarity, and style.

## What to do

$ARGUMENTS

**Argument forms:**
- *(no arguments)* — ask the user which files to edit before proceeding
- `all` — edit every `.md` and `.mdx` file in the repo (excluding `node_modules`, `.git`, `.claude`)
- A file path (e.g. `README.md`, `docs/api.md`) — edit that specific file
- A glob pattern (e.g. `docs/**/*.md`) — edit all matching files
- A file path + instruction (e.g. `README.md fix the link text`) — edit that file with a specific focus

If no arguments are given, ask: "Which files should I edit — all docs, or specific ones?"

---

## Editing Rules

1. Make sure all links to external resources are scannable and descriptive.

    **Use:** Check out more resources available in [our GitHub repository](https://github.com/saminacodes/mark-twain).

    **Avoid:** Check out this link [here](https://github.com/saminacodes/mark-twain)

2. Use simple tense for headings.

3. Double check callouts and notes for proper labeling.

    Valid callout labels:
    - Note:
    - Warning:
    - Info:
    - Tip:
    - Check:
    - Danger:

---

## Writing Style & Guidelines

> Fill in this section with your preferences. Delete any headings that don't apply.

**Voice & Tone**
<!-- e.g. "Direct and professional — no marketing language." -->

**Audience**
<!-- Who is the primary reader? -->

**Terminology**
<!-- Preferred terms or terms to avoid. -->

**Prohibited phrases or patterns**
<!-- e.g. "Never use 'simply', 'just', 'easy', 'straightforward'" -->

**Format preferences**
<!-- e.g. "Sentence case headings. Oxford comma. Code blocks must have language tags." --> 

**What NOT to change**
<!-- e.g. "Don't rewrite code examples." -->
<!-- e.g. "Don't change headings — they are linked externally." -->
